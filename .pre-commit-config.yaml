repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

  - repo: local
    hooks:
      - id: fixture-pii-scan
        name: scan committed fixtures for PII and secrets
        entry: python3 scripts/check_fixtures_pii.py
        language: system
        files: ^tests/fixtures/.*\.json$
        pass_filenames: false
